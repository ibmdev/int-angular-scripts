export declare function updateVersionPatch(tag?: boolean): Promise<{
    stdout: string;
    stderr: string;
}>;
export declare function updateVersionMinor(tag?: boolean): Promise<{
    stdout: string;
    stderr: string;
}>;
export declare function updateVersionMajor(tag?: boolean): Promise<{
    stdout: string;
    stderr: string;
}>;
